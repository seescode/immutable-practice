const Rx = require('rxjs');
const testSubject = new Rx.Subject();


/**
 * Debounce (debounceTime) by 1 second and multiply the result by 10.
 * https://www.learnrxjs.io/operators/filtering/debouncetime.html
 */

// YOUR CODE
testSubject;


testSubject.next(1);
testSubject.next(2);
testSubject.next(3);
