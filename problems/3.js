const check = require('../check.js');

/**
 * Problem: Add a new element to an array using spread operator
 */

var input = [1, 2, 3];

var expect = [1, 2, 3, 4];

// YOUR CODE
var yourCode;

check(input, yourCode, expect);