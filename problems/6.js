const check = require('../check.js');

/**
 * Problem: Sum the values of x and y using reduce()
 */

var input = [{ x: 1, y: 1 }, 
             { x: 2, y: 1 }, 
             { x: 3, y: 1 }];

var expect = 9;

// YOUR CODE
var yourCode;

check(input, yourCode, expect);